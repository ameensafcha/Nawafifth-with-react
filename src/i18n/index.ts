import { en } from './en';
import { ar } from './ar';
import { Translations } from '../types';

export const translations: { en: Translations; ar: Translations } = {
  en,
  ar,
};

export { en, ar };
